#' Determine if Conda is installed
#' @param folder_name name of the folder where all Python
#' environments and packages are installed
#' @export
is_conda_installed <- function(folder_name) {
  # ...
}
